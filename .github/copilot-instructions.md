After every change, commit each individual change in a separate commit message. 
If you can't solve a change after two attemps, you should add some temporary debugging lines, tell me to run the program, then read the debugging log, find a solution, then delete the debugging lines
